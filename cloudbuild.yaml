options:
  logging: CLOUD_LOGGING_ONLY

substitutions:
  _REGION: "europe-west1"
  _REPOSITORY: "trusted-images"
  _IMAGE: "webapp"
  _CLUSTER: "binaryauth-cluster"
  _ZONE: "europe-west1-b"

steps:

#######################################################
# BUILD
#######################################################

- id: Build
  name: gcr.io/cloud-builders/docker
  args:
    - build
    - "-t"
    - "${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/${_IMAGE}:$SHORT_SHA"
    - "."

#######################################################
# PUSH
#######################################################

- id: Push
  name: gcr.io/cloud-builders/docker
  waitFor:
    - Build
  args:
    - push
    - "${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/${_IMAGE}:$SHORT_SHA"

#######################################################
# CREATE ATTESTATION
#######################################################

- id: Sign Image
  name: gcr.io/google.com/cloudsdktool/cloud-sdk
  waitFor:
    - Push
  entrypoint: bash
  args:
    - -c
    - |
      gcloud alpha container binauthz attestations sign-and-create \
        --artifact-url=${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/${_IMAGE}:$SHORT_SHA \
        --attestor=trusted-cloudbuild \
        --keyversion-project=$PROJECT_ID \
        --keyversion-location=global \
        --keyversion-keyring=binaryauth-keyring \
        --keyversion-key=binaryauth-key \
        --keyversion=1

#######################################################
# GET GKE CREDENTIALS
#######################################################

- id: Get Credentials
  name: gcr.io/google.com/cloudsdktool/cloud-sdk
  waitFor:
    - Sign Image
  entrypoint: gcloud
  args:
    - container
    - clusters
    - get-credentials
    - "${_CLUSTER}"
    - "--zone=${_ZONE}"
    - "--project=$PROJECT_ID"

#######################################################
# PATCH MANIFEST
#######################################################

- id: Update Manifest
  name: ubuntu
  waitFor:
    - Get Credentials
  entrypoint: bash
  args:
    - -c
    - |
      sed -i "s|IMAGE_PLACEHOLDER|${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/${_IMAGE}:$SHORT_SHA|g" k8s/deployment.yaml

#######################################################
# DEPLOY
#######################################################

- id: Deploy
  name: gcr.io/cloud-builders/kubectl
  waitFor:
    - Update Manifest
  env:
    - CLOUDSDK_COMPUTE_ZONE=${_ZONE}
    - CLOUDSDK_CONTAINER_CLUSTER=${_CLUSTER}
  args:
    - apply
    - -f
    - k8s/

images:
  - "${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/${_IMAGE}:$SHORT_SHA"
